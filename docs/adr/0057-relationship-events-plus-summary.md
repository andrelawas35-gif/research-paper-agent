# Relationship Events Plus Summary

Relationship Management will store append-only Relationship Events and maintain a Derived Person Summary for lookup. Keeping only the latest summary would be simpler, but relationship memory is sensitive enough that the agent needs auditability, correction paths, and provenance for why it believes something about a Person.
