# Tool-Driven Adaptive Grill

The agent will generate personalized questions and recommendations through explicit tools (`adaptive_grill` and `respond_to_adaptive_grill`) instead of relying only on prompt instructions. This keeps the questioning loop auditable, one-question-at-a-time, and grounded in both the local User Model and cited paper text.
