# Infer Session Goals

The research agent should infer the session goal by default and ask one clarifying question only when ambiguity would materially change the output. This keeps interaction lightweight while preventing wasted work on unclear synthesis or recommendation tasks.
